import 'package:flutter/material.dart';

class CustomScrollViewDetail extends StatelessWidget {
  const CustomScrollViewDetail({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CustomScrollView();
  }
}
